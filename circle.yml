machine:
    python:
        version: 3.5.1
    #pre:
    #    - python3 -m pip install virtualenv


dependencies:
#    pre:
#        - pyenv local 3.5.1
    override:
        - python --version
        - pip --version
        - python -m pip install -r requirements.txt
        - pylint --version


test:
    override:
        - make test
