machine:
    python:
        version: 3.5.1

dependencies:
    override:
        - pyenv global 3.5.1
        - pyenv global
        - pyenv which python
        - pyenv which pip
        - python --version
        - which python
        - pip --version
        - which pip

        - pyenv which python3
        - pyenv which pip3
        - pyenv which pylint
        - python3 --version
        - which python3
        - pip3 --version
        - which pip3
        - which pylint
        - pylint --version
        
        #- python3 -m pip install -r requirements.txt
        #- pylint --version
        #- pip3 install -r requirements.txt
        #- pylint --version


test:
    override:
        - make test
