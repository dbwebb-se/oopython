machine:
    python:
        version: 3.5.1

test:
    pre:
        - make install

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    # gitter hook
    - url: https://webhooks.gitter.im/e/4d538d49c841f4f172d6
