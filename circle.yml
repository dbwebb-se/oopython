machine:
    python:
        version: 3.5.1



dependencies:
    override:
        #- sudo pip3 install -r requirements.txt
        - whereis pip3
        - pylint --version
        - pip --version
        - python --version
        - pip3 --version
        - python3 --version
        - pip3 install -r requirements.txt



test:
    override:
        - make test
