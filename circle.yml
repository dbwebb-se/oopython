machine:
    python:
        version: 3.4.3



dependencies:
    pre:
        - sudo apt-get install python3-pip
    override:
        #- sudo pip3 install -r requirements.txt
        - whereis pip3
        - pip --version
        - python --version
        - pip3 --version
        - python3 --version
        - pip3 install -r requirements.txt
        - pylint --version


test:
    override:
        - make test
