machine:
    python:
        version: 3.4.3
    pre:
        - python3 -m pip install virtualenv


dependencies:
    override:
        - python3 -m pip install -r requirements.txt
        - pylint --version


test:
    override:
        - make test
